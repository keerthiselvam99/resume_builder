export const environment = {
  production: false,
  apiUrl: '/api/v1',
  /**
   * E2E track that exercises the real HTTP repositories against a backend
   * running on the Angular dev-server proxy (/api -> localhost:3000).
   */
  useMockApi: false,
};
